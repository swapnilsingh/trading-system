apiVersion: apps/v1
kind: Deployment
metadata:
  name: indicator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: indicator
  template:
    metadata:
      labels:
        app: indicator
    spec:
      containers:
        - name: indicator
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
      nodeSelector:
{{ toYaml .Values.nodeSelector | indent 8 }}
