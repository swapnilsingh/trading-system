[pytest]
asyncio_mode = strict
addopts = --strict-markers --tb=short --maxfail=5 --disable-warnings
testpaths = tests
pythonpath = .
markers =
    asyncio: mark test as asyncio-compatible
    skip: mark test to be skipped
    integration: mark test as an integration test
    unit: mark test as a unit test
